node_modules/
dist/
.env
# External dataset samples (SWE-chat, etc.) pulled for the Advance
# offline-evaluation fixture set — never committed, licensed for local
# research/product use under the dataset's own gated-access terms, not
# for redistribution via this repo.
.data-cache/
