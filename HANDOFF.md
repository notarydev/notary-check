# Notary Check — handoff brief for continuing the build

> This is a running build log, kept for narrative history — it is not
> governed by `docs/README.md`'s status-header system. For current rules
> and current state, start at [`CLAUDE.md`](CLAUDE.md) instead. This file
> stays useful for "what happened and why," not "what's true right now."

Paste this file's contents (or point at this file) when handing the work to another coding agent (opencode or otherwise). It exists so the next session doesn't have to re-derive context from this repo's commit history.

## Resume protocol — if this session ends, start here

This file is written so that **if this session dies mid-work and the next instruction is just "continue," any agent — Claude, or opencode running on DeepSeek — can pick up exactly where it left off without the person having to re-explain anything.**

1. **Git is ground truth, not this file.** This file is updated after every completed step, but if it's ever stale, `git log --oneline --all` and `git branch -a` on `github.com/notarydev/notary-check` tell you what's actually landed. Check `## Progress log` below first; verify against git if anything looks off.
2. **Find the next unchecked item in `## Progress log`.** Work proceeds top to bottom, in the order given — later steps depend on earlier ones being real (this is `§ Phase 1 build order`'s own stated discipline, not an arbitrary choice).
3. **Do the work on a feature branch** (`feature/<step-name>`), off `main`, following the pattern already used for `feature/evidence-manifest`.
4. **The dispatch pattern that's worked so far:**
   ```bash
   opencode run "<scoped task, referencing docs/build/tier-1-build-and-operating-plan.md section numbers>" \
     -m deepseek/deepseek-v4-flash --auto --format json --title "<step name>"
   ```
   Scope each dispatch to exactly one `§ Phase 1 build order` step — no more. Tell it explicitly what NOT to build (the next steps down the list, auth, the judge, etc.) so it doesn't silently expand scope.
5. **Never trust "typecheck passed" as "it works."** Every step so far has had at least one real bug that only showed up when actually run against a live dependency (a wrong relative path in the migration runner, invalid UUIDs in seed data that only failed at request-validation time). Stand up the real dependency (Postgres via `docker run`, etc.), exercise every case by hand (`curl`, not just reading the code), and only then commit.
6. **Update `## Progress log` below before ending the session** — check off what's done, note what was verified and how, note any bugs found/fixed. A step that's "done" in code but not reflected here has not actually been handed off.

## Source of truth

The full spec is one file: **`docs/build/tier-1-build-and-operating-plan.md`** (checked into this repo). Everything below is a pointer into that document. Do not re-derive product decisions, card copy, or the verification pipeline design from scratch — they're already decided and cross-referenced there.

## Repo structure — the engine/MCP boundary

`§ Architecture` in the plan draws a real seam between two components. This repo keeps both in one place (a monorepo, not separate repos, and not git branches — branches are for versioned changes over time, not for separating two components that run simultaneously) but enforces the boundary in code:

- **`server/` + `ui/` — the MCP layer.** Thin and stable. Its only job is: receive the tool call, forward it to `engine/`'s API, render the card. Changes rarely once the card/tool contract is locked (`§ Product contract`).
- **`engine/` — the orchestrator.** Everything in the plan's architecture diagram under "review orchestrator + queue": the evidence manifest, deterministic verifier, judge integration, Postgres data model, quotas, monitoring. This is where nearly all of `§ Phase 1 build order`'s work happens.

**Hard rule: `server/` calls `engine/` only over its HTTP API, never by importing its internals.** That's what makes a future split into separate repos (if a second client — a browser extension, the later WATCH gateway — ever needs the engine) a clean extraction instead of an untangling job. Split into separate repos only when there's an actual second consumer or a real reason for divergent deploy cadence — not preemptively.

Use normal feature branches for units of work (`feature/evidence-manifest`, `feature/deterministic-verifier`, ...), merged to `main` after review.

## Progress log — the actual resume point, keep this current

**Phase 0** (§ Phase 0 build guide) — local build done and verified; live/human steps not done:
- [x] `server/` + `ui/` scaffolded, built, and verified — MCP server boots and routes all four mocked scenarios correctly; card renders correctly (copy, layout, special characters) via the `?mock=` param. Several real bugs found and fixed along the way (ext-apps import path, Vite/`vite-plugin-singlefile` version incompatibility, entry-renaming approach, mock-param double-decode, charset/background — see `README.md`).
- [ ] Expose the server publicly (Cloudflare Tunnel or ngrok) — § 0.10.
- [ ] Register the tunnel URL as a Claude custom connector; confirm all four scenarios render through a *live Claude conversation*, not just the local browser test — § 0.11.
- [ ] 20–30 scripted test conversations with real people — § 0.12. This is a people/scheduling task, not something a coding agent can do.

**Phase 1** (§ Phase 1 build order — work top to bottom, each step depends on the last being real):
- [x] **Step 1 — source manifest binding + immutable locator/snapshot layer.** `engine/` package added: `Evidence` table exactly per § Core data model (14 fields), minimal `Organization`/`Review` stubs for FK integrity, `POST /v1/evidence` (org-scoped, append-only, inline payloads SHA-256 hashed). **Merged to `main`.** Verified for real against a live Postgres container: migrations applied, all 6 request cases exercised via `curl` (missing auth, cross-org rejection, url-only, inline-payload hashing, validation failure, invalid enum), append-only guarantee confirmed by resubmitting the same URL and checking two distinct rows landed. Two real bugs found and fixed in review: a wrong relative path in the migration runner, and non-RFC-compliant seed UUIDs that failed the route's own Zod validation.
- [x] **Step 2 — deterministic claim-field checks + the state machine** (§ Verification pipeline steps 2, 5, 8). `engine/src/verification/applicability.ts` (pure `assessApplicability()` per step 5 — a material field mismatch excludes a candidate even when its value is attractive; a differing value on an otherwise-matching candidate is a contradiction, not an applicability failure) and `stateMachine.ts` (pure `assignState()` per step 8's exact precedence; `CONFLICTED` intentionally excluded — CAPTURE-tier only). `Claim`/`EvidenceMatch` tables added (migration 0003), schema only — nothing reads/writes them yet; that's later steps. Claim *extraction* from raw text is explicitly out of scope here (separate, likely model-backed, later work) — these modules take already-structured fields. **Merged to `main`.** 17 unit tests (`node:test`, no new dependency), independently re-run (17/17 pass), typecheck/build independently re-run clean, migration independently re-verified against a fresh Postgres container. One documentation gap fixed in review: `stateMachine.ts` didn't state strongly enough that callers must check `applicable === true` before using `valueConflicts` to classify a relation — an inapplicable-but-value-differing candidate must never be wired in as "contradicts." Fixed with an explicit caller-precondition comment before merge.
- [x] **Step 3 — adversarial golden fixtures for source ingestion** (§ Verification pipeline step 3; locked test cases 16–17). `engine/src/ingestion/safeFetch.ts` — scheme allowlist, private/reserved-address denial (incl. 169.254.169.254), DNS-rebinding defense (connection pinned to the validated IP via a `lookup` override — hostname never re-resolved between validation and connect), manual redirect following re-validated on every hop, byte cap (declared + streamed), decompression-bomb protection (independent decompressed-size cap), overall wall-clock timeout, MIME allowlist per § Document-class scope for v1. `delimitEvidence.ts` — case 17's prompt-injection-safe fencing (random-nonce fence + escaping of fence-shaped content already inside the evidence; makes no claim about model behavior, only structural soundness — no model is wired in yet). `fixtures/hostilePdf.ts` — case 16's hostile-file builders, explicitly scoped to what's provable at the fetch boundary (oversized/slow), with deep parser-hardening honestly deferred since no PDF/office parser is wired in yet. **Merged to `main`.** 48 tests total, every one spinning up/tearing down a real local `node:http` server (only DNS is mocked, and only where determinism requires it) — independently re-run, 48/48 pass. Reviewed with extra scrutiny as security-critical code: read every line of both modules and both test files in full. One real bug found and fixed: `Content-Encoding: identity` (a legitimate RFC 7231 value) was being rejected as unsupported, which would have wrongly marked a real report server as unavailable — fixed, with a new test. Independently verified the DNS-rebinding test is genuinely strong (targets an RFC 2606 `.invalid` hostname, intercepts the real socket's `remoteAddress`) rather than trusting the dispatched report.
- [x] **Step 4 — the constrained judge** (§ LLM judge design — DeepSeek, Chain-of-Verification, the four-outcome extraction vocabulary in § Judge authority boundary, no raw confidence gating). `engine/src/judge/judgeClient.ts` (thin `deepseek-v4-flash` client, no `tools`/retrieval anywhere in the request — closed authority is architectural, not a prompt instruction), `fieldExtraction.ts` (`extractField()` — blind by construction, no parameter exists for the claim's value; evidence always routed through step 3's `delimitEvidenceForModel`; strict-zod four-outcome parsing, no confidence field, a sneaked-in confidence key collapses to `cannot_be_determined`), `promptTemplates.ts` (per-field literal procedures, § Writing the judge's instructions' four-part structure). **Merged to `main`.** 78 tests (76 mocked + 2 live-API, live tests skip cleanly without a key) — independently re-run, including the live-API tests for real against the actual DeepSeek API (a key was available), both passed. `integration.test.ts` proves the core invariant two ways: statically (source-scanning — the judge modules never call or import the state machine) and behaviorally (flipping one mocked judge answer flips `SUPPORTED → CONTRADICTED → UNSUPPORTED` purely through step 2's pure functions).
  - **Follow-up needed, found by live end-to-end testing, not fixed here:** ran the flagship scenario through the real pipeline with a realistically-paraphrased evidence passage (same facts, different wording — "fiscal 2025" vs. claim's "FY25", "year over year" vs. "prior year"). Every individual piece worked exactly as specified, but composed together the result was a false inapplicable/`UNSUPPORTED` — step 2's exact-string comparator (already reviewed/approved) doesn't tolerate wording differences, and step 4's judge extracts values "as written," never normalized. This will likely misfire often against real evidence, since real reports rarely phrase things identically to how a claim states them. `docs/build/tier-1-build-and-operating-plan.md`'s `Claim.decontextualized_form` field hints at an intended normalization step. **PARTIALLY CLOSED as of 2026-09-01 — see the progress-log entry below (`comparator/wording-normalization gap, partially closed (v1 scope)`) for the full record.** A typed, allow-listed normalization module (`engine/src/verification/normalization.ts`) now sits between the judge's raw field extraction and step 2's comparator, covering v1's bounded scope: safe-syntax differences (case, punctuation, whitespace, Unicode), corporate-suffix spelling variants ("Acme, Inc." ~ "ACME Inc"), percent notation ("12 percent" ~ "12%"), numeric grouping separators ("12,000,000" ~ "12000000"), explicitly-declared value multipliers ("m" ~ ",000,000"), and fiscal-year LABEL formatting ("FY25" ~ "fiscal 2025", as text only — never calendar-date math). What remains un-closed is by design, not oversight: semantic paraphrase ("gross revenue" never equals "revenue"), true entity-alias resolution beyond suffix spelling, and fiscal-calendar date conversion are all explicitly out of v1 scope. This maps to locked test cases 9 and 10, now covered by tests in `engine/src/verification/applicability.test.ts` and `engine/src/verification/normalization.test.ts`.
- [x] **Step 5 — auth, quotas, retention/deletion, observability (§ Monitoring), kill switch.** `engine/src/auth/apiKey.ts` replaces step 1's `x-notary-organization-id` header stub with real `Authorization: Bearer <key>` auth (SHA-256-hashed keys, never stored plaintext; immediate hard-fail on revocation) — `evidence.ts`'s business logic is byte-for-byte unchanged, only the identity mechanism moved. `UsageEvent` (exactly per § Core data model) + `quotas/quotaCheck.ts` (a per-org monthly limit AND a separate hard global DeepSeek spend cap — the global cap is a real second gate, not redundant, since a fleet of orgs each under their own limit can still blow the aggregate bill). `judge/killSwitch.ts` (`NOTARY_JUDGE_KILL_SWITCH`, enforced at `extractField`'s one real chokepoint, before a judge client is even constructed). `evidence/deleteEvidence.ts` (honest deletion — nulls payload fields, sets `access_revoked_at`, keeps the row; migration 0004 also fixes a real self-caught issue: the original resolvable-content CHECK from step 1 would have blocked honest deletion of an inline-payload-only row). `observability/log.ts` (minimal structured JSON logging, no platform dependency, wired into both the evidence route and the judge call site). **Merged to `main` — this closes out Phase 1's code.**
  - **Explicitly deferred, not silently built — same treatment as the Stripe/AWS/Cloudflare decisions earlier:** full OAuth/OIDC for a human-facing dashboard (needs a vendor: Auth0/Clerk/WorkOS/custom) and a metrics/alerting platform integration (Datadog/Grafana/etc.). The API-key auth built here is service-to-service, not a human login system.
  - 99 tests (97 pass + 2 skipped live-API), 18 of them real-Postgres-backed. Independently re-verified: fresh (not reused) Postgres container, all 4 migrations applied and schema-diffed against § Core data model, full suite re-run, plus my own live manual exercise of the auth swap against a running server (valid key → 201, old header stub → confirmed genuinely gone → 401, garbage/missing bearer → 401, revoking a key live → immediate 401 on the next request) and direct inspection of the structured logs (correct shape, no key material anywhere).
  - **Known gap, honestly documented in code, not papered over:** no caller in the codebase yet persists judge usage to `usage_event` — `quotas/usage.ts`'s mapping is real and tested, but the write only happens once an orchestrator that owns review/org context exists to call it. `quotaCheck()` itself is real and independently DB-tested.
- [ ] **Step 6 — a tightly scoped invited cohort.** An ops/people step, not code — see docs/build/tier-1-build-and-operating-plan.md's Experiment design and Delivery sequence sections when this is ready to start.

**Phase 1's code (steps 1–5) is done.** Two things need a human decision before real evidence/real users hit this, tracked here so they aren't lost: (1) the comparator/wording-normalization gap noted under step 4, and (2) picking an observability platform, noted under step 5.

## Beyond Phase 1 — work done ahead of `docs/build/tier-1-build-and-operating-plan.md`'s own sequencing, at the product owner's explicit request

`§ Delivery sequence` puts billing and self-serve auth after an invited cohort and the pre-pilot engine gate. That sequencing is still correct as *launch* advice — none of what's below should be described as "live" until those gates are real — but the integration code was requested now, so it's built now:

- [x] **Stripe billing scaffolding.** `engine/src/billing/` — checkout + webhook routes, idempotent Product/Price bootstrap, four tiers (Starter $0/25 checks, Pro $49/500, Team $199/2,500, Enterprise custom). **Merged to `main`.** Independently re-verified against the live Stripe test account (not just the dispatched report): products/prices listed directly via the Stripe API, amounts confirmed, bootstrap re-run independently and confirmed byte-identical idempotent output. 127 tests (125 pass + 2 skipped live-DeepSeek).
- [x] **Clerk human-dashboard auth.** `dashboard/` — a new Next.js (App Router) app, scaffolded manually via `create-next-app` + the official Clerk quickstart rather than `clerk init` (the CLI's device-flow login never completed in-session; the manual path is equivalent). `ClerkProvider` wired in `layout.tsx`; sign-in/sign-up/`UserButton` controls on the landing page. **Merged to `main`.**
  - **Real bug found and fixed, not by guessing:** this Next.js version (16.3.4) renamed the `middleware.ts` file convention to `proxy.ts` (Next v16.0.0+) — the standard Clerk setup instructions assume the old name, which Next.js no longer recognizes at all (would have silently failed to run, breaking auth with zero error output). Built as `dashboard/src/proxy.ts`; confirmed via `next build` reporting `Proxy (Middleware)` correctly picked up.
  - **Live verification:** this Clerk instance is **production** (`pk_live_`/`sk_live_`, not test) with a custom Frontend API domain, `clerk.getnotary.ai`. That CNAME didn't exist in Cloudflare DNS initially — confirmed missing via `dig`, then confirmed added and resolving via `dig @1.1.1.1`/`dig @8.8.8.8` once the user added it. The user independently confirmed sign-in/sign-up working in a real browser; my own sandboxed browser tool still can't resolve the domain (a stale DNS cache local to that tool's own container, confirmed by direct `dig` against public resolvers succeeding while the tool's browser fails) — noted honestly rather than claimed as independently re-verified end-to-end.
  - Real Clerk keys live only in `dashboard/.env.local` (gitignored, never committed).
- [x] **The review orchestrator.** `Review.idempotency_key` (migration 0006 — `idempotency_key`, `status`, `created_at`, `completed_at`, a partial unique index on `(organization_id, idempotency_key) WHERE idempotency_key IS NOT NULL`, so idempotency is scoped per-org, not global; locked test case 18) and `evidence.resolved_text` (a deliberate, narrow stand-in for the real S3 payload store, documented as such in the migration — needed because without persisting *something* retrievable, the flow below has nothing to read back for a previously-resolved evidence row). `engine/src/ingestion/resolveEvidence.ts` wires `safeFetch` into evidence resolution for real — lazily, on demand from the review flow (NOT from `POST /v1/evidence`, to keep registration fast and decoupled from an arbitrary remote fetch's latency); HTML is canonicalized with a simple tag-stripper, PDF has no parser wired in anywhere in this codebase so `resolved_text` stays `NULL` and raw bytes are hashed instead. `engine/src/review/reviewFlow.ts`'s `runReview()` is the real end-to-end flow: resolves bound evidence, runs a deterministic exact-substring field pass first, calls the judge only for the residue (gated once per review by `checkQuota` — a denied quota resolves residual fields to `cannot_be_determined` with no network call, same shape as the kill switch), assembles `EvidenceFields`, runs `assessApplicability`, builds relations only for applicable rows, calls `assignState`, and persists `claim` + `evidence_match` in one transaction — finally closing step 5's known gap: a usage_event is now written for every judge call that actually reaches the network. `engine/src/routes/reviews.ts` exposes `POST /v1/reviews` (idempotent create, race-safe under concurrent identical requests — tested both sequentially and concurrently) and `POST /v1/reviews/:reviewId/claims` (thin wrapper over `runReview`, explicitly NOT claim-level-idempotent — `recheck_claim` is separate, later, out-of-scope work). **Merged to `main`.**
  - 147 tests total on merged `main` (119 new + 28 existing billing, 0 skipped) — independently re-verified against a brand-new Postgres container the dispatched agent never touched: all 6 migrations (including billing's `0005_billing.sql`, merged to `main` after this branch started — no schema overlap, this work's migration was renumbered to `0006` before merging) applied from empty, `typecheck`/`build`/full suite re-run clean on the merged tree. Ran a manual `curl` pass against a running server: duplicate-idempotency-key returns the same review (200, not 201); a real public URL (`https://example.com/`) registered `pending` and lazily resolved to `retrieved` with real `canonical_url`/`resolved_text` only once a claim actually needed it; a claim against both that URL and a verbatim inline payload came back `SUPPORTED` with the expected single match, persisted correctly in `claim`/`evidence_match`.
  - **Two real bugs found in independent review, not caught by the dispatched agent's own tests:** (1) `resolveEvidenceRow` had no locking around its check-fetch-write sequence — three concurrent calls on the same `pending` row produced three real HTTP fetches, not one, confirmed by an ad hoc concurrent-call script against a live Postgres + HTTP server before any fix existed. Fixed with `SELECT ... FOR UPDATE` around the whole sequence; re-ran the same script post-fix, 1 hit consistently across 8 trials; added a permanent regression test. (2) A malformed `reviewId` route param crashed the claims route to a generic 500 (an unhandled "invalid input syntax for type uuid" from Postgres) instead of a clean 400 — found by manually curling the running server with garbage input. Fixed by validating the route param before any query runs; added a permanent regression test.
  - **Same open follow-up as step 4, not re-litigated or fixed here:** the comparator/wording-normalization gap. In this design a judge-derived field can only ever surface as a *contradiction* (a value that differs from the claim's own) — a judge-derived *support* is structurally unreachable, since if the judge's extracted value matched the claim's literal string, the deterministic pass would already have found it first. Real evidence phrased differently from a claim's exact wording will still misfire toward `UNSUPPORTED`/`INDETERMINATE` rather than `SUPPORTED`, for the same underlying reason step 4 flagged. **PARTIALLY CLOSED as of 2026-09-01 — this is the same fix as the step-4 follow-up above, not a second one; see the progress-log entry below (`comparator/wording-normalization gap, partially closed (v1 scope)`).** A typed, allow-listed normalization module (`engine/src/verification/normalization.ts`) now covers v1's bounded scope: safe-syntax differences (case, punctuation, whitespace, Unicode), corporate-suffix spelling variants ("Acme, Inc." ~ "ACME Inc"), percent notation ("12 percent" ~ "12%"), numeric grouping separators ("12,000,000" ~ "12000000"), explicitly-declared value multipliers ("m" ~ ",000,000"), and fiscal-year LABEL formatting ("FY25" ~ "fiscal 2025", as text only). Semantic paraphrase, true entity aliases, and fiscal-calendar conversion remain explicitly out of scope by design, not oversight. This narrows — does not eliminate — the misfire above: representation-level wording differences now resolve deterministically, while genuinely semantically-different phrasing still lands `UNSUPPORTED`/`INDETERMINATE`.

- [x] **2026-09-01 — comparator/wording-normalization gap, partially closed (v1 scope).** This is the fix cross-referenced from the two gap-description edits above (under Step 4 and the review orchestrator) — one fix, not two. New module `engine/src/verification/normalization.ts` (typed, allow-listed normalization — normalize representation, never meaning) wired into `engine/src/verification/applicability.ts`'s pure `assessApplicability()`, replacing its bare `normalize()` calls; tests in `engine/src/verification/applicability.test.ts` and the new `engine/src/verification/normalization.test.ts`. Handles: safe-syntax differences (case, punctuation, whitespace, Unicode), corporate-suffix spelling variants ("Acme, Inc." ~ "ACME Inc"), percent notation ("12 percent" ~ "12%"), numeric grouping separators ("12,000,000" ~ "12000000"), explicitly-declared value multipliers ("m" ~ ",000,000"), and fiscal-year LABEL formatting ("FY25" ~ "fiscal 2025", as text only — never calendar-date math). `evidence_match.applicability_json` (existing jsonb column, no migration) now additionally carries per-field `normalizedClaimed`/`normalizedEvidence`/`rule` where a rule fired. Deliberately out of scope by design, not oversight: semantic normalization of measure/comparatorBaseline/modality/scope ("gross revenue" never equals "revenue"), real entity-alias resolution beyond suffix spelling (unwired `resolveEntityAlias()` stub is the future seam), fiscal-calendar date mapping (unwired `resolveFiscalCalendarMapping()` stub), and any fuzzy/similarity matching. Locked cases 6/7/8 (wrong entity/period/denominator) untouched and passing byte-for-byte as a regression guard; cases 9 and 10 (semantic paraphrase that supports / remains indeterminate) now covered by tests. **Verified:** full engine suite run independently — 194 tests, 143 pass, 0 fail, 51 skipped (pre-existing DB/API-key-gated skips), plus `typecheck`/`build` clean. **Real bug found and fixed in review:** the entity-corporate-suffix rule-application logic applied/reported the suffix rule without an exact allow-listed trailing-suffix match — a trailing token that merely resembled a suffix (e.g. "Acme US") risked being misclassified or folded, which would have been a false-supported risk. Fixed by gating both the rewrite and the `entity-corporate-suffix-v1` rule id on a literal allow-listed suffix token, with permanent regression tests (`normalizeEntity never folds different qualifiers into the same entity`; locked case 10's "Acme plc" vs "Acme US" stays excluded).

- [x] **2026-09-02 — dashboard read endpoints.** Org-scoped, read-only routes added to `engine/` so `dashboard/` (built separately, by a parallel agent — not touched here) can show review history, an evidence library, usage/quota, plan info, and API-key management. Migration `0008_dashboard_reads.sql` adds `claim.created_at` and `evidence.created_at` (neither table had ANY timestamp column before — both PKs are random `gen_random_uuid()`, so there was no correct way to paginate or to answer "checks this month" without one) plus three supporting indexes. `auth/apiKey.ts` gained `listApiKeys()` (never returns `key_hash`), unit-tested. New routes: `GET /v1/reviews` (keyset-paginated review history, optional `status` filter), `GET /v1/reviews/:id` (one review + its claims + each claim's `evidence_match` rows joined to `evidence`, 404 not 403 cross-org), `GET /v1/evidence` (org-scoped evidence library via `JOIN review`, deliberately excludes `resolved_text`), `GET /v1/usage`, `GET /v1/organization`, and `GET`/`POST`/`DELETE /v1/api-keys`. All follow the existing `Authorization: Bearer` → `verifyApiKey()` → org-derived-from-key pattern exactly; nothing here writes `claim.state` or touches the verification pipeline. Mounted in `server.ts`. Not committed — left unstaged per instruction.
  - **Real verification, not just typecheck:** `tsc --noEmit` clean, then a fresh local Postgres (`notary-check-pg`, already running via `docker`) got `0008` applied (`applied 0008_dashboard_reads.sql`), columns/indexes confirmed via `\d claim`/`\d evidence`/`\di`. Full suite: **207 tests, 203 pass, 0 fail, 4 skipped** (pre-existing DeepSeek-key-gated skips), up from 151/206 on a sandbox with no live DB — this run had a real Postgres, so nearly everything that used to skip actually ran. Then a running server was driven with real `curl`: valid key on every new route, missing/malformed/revoked-key 401s, cross-org 404 on `GET /v1/reviews/:id` and `DELETE /v1/api-keys/:id` (confirmed the *other* org's data/key is unaffected, not just that the response was 404), `POST`/`GET`/`DELETE /v1/api-keys` round-tripped including idempotent double-revoke (200 twice) and a 404 on a nonexistent key id, `GET /v1/evidence` confirmed to omit `resolved_text` from every row, and `GET /v1/usage` confirmed to increment `checks_used_this_month` after a real claim was posted.
  - **Real bug found and fixed, not by guessing:** the very first cursor-pagination test (`GET /v1/reviews?limit=2` then following `next_cursor`) 500'd with a Postgres error — `time zone "gmt-0400" not recognized`. Cause: `node-pg` returns `timestamptz` columns as JS `Date` objects at runtime, not strings, despite the route code's own `created_at: string` type annotation (pg doesn't narrow the type, only the runtime value). The cursor-encoding helper did manual template-literal string concatenation on that value, which silently called the `Date`'s locale-formatted `.toString()` (`"Wed Sep 02 2026 10:59:04 GMT-0400 (Eastern Daylight Time)"`) instead of `.toISOString()` — not a valid `timestamptz` literal, so the second page's query threw and the app's generic error handler turned it into an opaque 500 (the exact same failure *shape* HANDOFF.md's review-orchestrator entry already flagged for malformed-UUID route params — caught here the same way, by actually driving a running server, not by reading the code). Fixed in both `reviews.ts` and `evidence.ts` by calling `new Date(last.created_at).toISOString()` before encoding the cursor; re-ran the same two-page pagination curl sequence post-fix and it now returns the correct next page with a `next_cursor: null` on the final page.

- [x] **2026-09-02 — dashboard foundation + public signup gate.** Two more pieces of the same SaaS-dashboard push as the entry above, built directly (not dispatched) in parallel with it, touching only `dashboard/` plus one new engine route.
  - **Dashboard foundation** (`dashboard/`): fixed a real correctness bug before it could compound — `resolveOrganization()` mints a brand-new API key on *every* call (deliberate on the engine side, see `internal.ts`'s own comment), so every page load/action in a growing multi-page dashboard would otherwise mint a fresh, permanently-live, unrevoked key. Fixed via `dashboard/src/lib/currentOrg.ts`: caches the resolved org+key on the signed-in Clerk user's own `privateMetadata` (never sent to the browser — server-only field) after the first resolve, reading it back on later requests instead of re-minting. `account/page.tsx`/`actions.ts` refactored to use it (behavior-preserving; the redirect()-inside-try/catch trap actions.ts already warned about was deliberately avoided in the refactor by keeping the unauthenticated-redirect check outside any try/catch). Adopted shadcn/ui (`npx shadcn@latest init` + `sidebar dropdown-menu dialog table tabs badge card sonner`) — confirmed it correctly extended, not replaced, the existing Tailwind v4 CSS-first theme in `globals.css`. One real regression caught and fixed from the init itself: it switched dark mode from an automatic `prefers-color-scheme` media query to a class-based `.dark` strategy with nothing toggling that class, which would have silently broken dark mode for every user relying on system preference — fixed by wiring `next-themes` (`ThemeProvider attribute="class" defaultTheme="system" enableSystem`) into `layout.tsx`. Also found and fixed a second, smaller init artifact: `--font-sans: var(--font-sans)` in the new `@theme inline` block was self-referential (never resolves), silently breaking the Geist font previously wired up — pointed it back at `--font-geist-sans`. New `dashboard/src/app/dashboard/` route tree (`layout.tsx` with a real sidebar nav + `SidebarProvider`/`SidebarInset`, `page.tsx` plus `reviews/`, `evidence/`, `usage/`, `settings/` — each a "coming soon" placeholder naming the exact engine endpoint it will read once wired, not empty stubs). Verified with `npx tsc --noEmit`, `npm run lint` (one real lint error in shadcn's own generated `use-mobile.ts` — a `setState`-in-effect — fixed with a lazy `useState` initializer instead), and `npx next build` (clean, all 8 routes compile) — no live Clerk session available in this environment, so the privateMetadata caching path is verified by reading the actual installed `@clerk/backend` type defs (`updateUserMetadata`, `User.privateMetadata`) rather than by an end-to-end live run; flagging that honestly rather than claiming more than was actually exercised.
  - **Public signup gate**: the canonical plan blocks self-serve signup/payment until the held-out eval gate passes (it hasn't — see `docs/build/architecture-and-progress.md`). Two independent, both-required layers: (1) app-level — `dashboard`'s landing page reads `NOTARY_SIGNUP_MODE` (default `"waitlist"`; only exactly `"open"` enables real Clerk sign-up), rendering a `WaitlistForm` client component (`useActionState` + a `joinWaitlist` server action) instead when gated; sign-in stays available either way. (2) IdP-level — Clerk's own Restricted sign-up mode, a manual Clerk-dashboard setting outside this repo, **not yet confirmed set**, named here so it isn't lost. New engine route `POST /v1/waitlist` (`engine/src/routes/waitlist.ts`, migration `0009_waitlist.sql` adding `waitlist_signup`) — deliberately unauthenticated (no org exists yet), upsert-by-email so a repeat submission can't be used to probe list membership (always 201 on a valid email), a simple in-process per-IP rate limit (5/60s) as the only abuse resistance since there's no API key to gate on. **Real verification**: migration applied against the same live local Postgres (`\d waitlist_signup` confirmed), server driven with `curl` — valid signup 201, duplicate email still 201 with only one row in the table, invalid email and missing body both 400, and 6 rapid requests from one IP produced exactly one 201 then five 429s, matching the configured limit. 5 new tests added (`engine/src/routes/waitlist.test.ts`, same real-Postgres pattern as `internal.test.ts`) — full suite re-run after: 212 tests, 208 pass, 0 fail, 4 skipped.
  - All of the above left unstaged, same as the entry above.

- [x] **2026-09-02 — live-endpoint verification pass, and one real live bug found.** Before this, every claim in `docs/build/architecture-and-progress.md` about `server/`'s deployment was inferred from `.env` files and source, never from actually calling the deployed service — corrected by driving `https://mcp.getnotary.ai/` directly with the real MCP protocol (`initialize`, `tools/list`, four isolated `tools/call` invocations of `review_source_backed_answer`).
  - **Corrected, not previously known**: `server/` IS live at `mcp.getnotary.ai` (previously logged as "no evidence anywhere in this repo") — but running an **older build with no Clerk auth at all** (no 401 challenge, no OAuth discovery routes), meaning this session's Clerk/org-resolution work is not what's deployed there yet. `clerk.getnotary.ai` is real (previously "prose only," now `dig`-confirmed). `notarycheck.ai` (the `/account` mailto domain) does NOT resolve. **New discovery**: a live, real marketing site exists at `getnotary.ai` — genuine copy, Cloudflare-fronted — that doesn't match this checkout's `dashboard/` at all. Per the product owner, this is a known, older asset that needs a refresh — tracked as a backlog item below, not investigated further this session.
  - **A real, live bug found** by isolating one variable at a time across four `tools/call` requests: exact-wording support → correct; paraphrased support (percent notation + fiscal-year label, both Tier A.5 normalization) → correct, confirming the judge/normalization stack is genuinely live, not just configured; exact-wording contradiction → correctly `CONTRADICTED`; **paraphrased contradiction** ("declined 12 percent in fiscal 2025" vs. "grew 17% in FY25" — an operator paraphrase combined with a differing value) → came back `UNSUPPORTED` instead of `CONTRADICTED`. `judge/promptTemplates.ts`'s own `operator` field instructions explicitly authorize recognizing "declined" as `decrease`, so this looks like a live gap, not a scope boundary. **Not root-caused this session** — flagged, not diagnosed. This maps directly to locked test case 2, the project's own flagship contradiction scenario, so treat it as higher priority than dashboard UI work. Open question before fixing: is this already fixed in the current (undeployed) codebase and just needs a deploy, or a genuine live gap in how judge-derived `operator` values feed contradiction detection, or the judge silently not being invoked for this field (quota/kill-switch fallback)? A local repro was attempted but abandoned mid-session — the local `POST /v1/internal/resolve-organization` endpoint used to mint a debug key doesn't exist in the older deployed build, so a local repro through that path would test different code than what's actually live; a valid repro needs either a debug key minted a different way, or reasoning from source + local engine logs directly rather than through the internal-resolution endpoint.
  - `docs/build/architecture-and-progress.md` updated in the same pass with corrected domain-status table, a new "Live verification" section with the full four-case breakdown, and corrected "Actually live, verified" / "Configured, not confirmed live" lists.

- [x] **2026-09-02 — five confirmed correctness bugs from the external audit, closed engine-side.** Migration `0011_locators_lifecycle_revocation.sql` (renumbered from 0010 mid-session: a concurrently-running agent in this same checkout added its own `0010_entitlement.sql`, and its migrate run picked up this file while it was still being edited — caught by diffing the applied schema against the file, then rolled back and re-applied cleanly as 0011). All five verified against a real local Postgres, the real DeepSeek API, and `curl` against a running server — not by typecheck.
  1. **Real exact locators.** New `engine/src/evidence/locators.ts` — pure coordinate systems per evidence class (character offsets into canonical text; a restricted JSONPath for structured payloads; explicit `caller_supplied` provenance for inline excerpts) plus `resolveLocator()`, which **re-dereferences** a stored locator against the retained text. `evidence_match` previously held only a URL or `inline:<hash>`, and resolution happened once at write time and was trusted forever after. New `engine/src/ingestion/parsePdf.ts` (`pdf-parse`) gives PDFs real extracted text for the first time — page-level coordinates, honestly NOT bounding boxes. A judge-derived value that cannot be found in the retained text can no longer establish a field at all; `operator`/`modality` are the documented exemption (their values are closed-vocabulary classifications, not quotations) and can never carry a match on their own.
  2. **Per-claim lifecycle states.** `engine/src/review/lifecycle.ts`; `extractClaims()` now returns a discriminated `{ok:true,claims}` / `{ok:false,reason}` instead of degrading every failure to `[]`; `runReview()` returns and persists `lifecycle_state`/`lifecycle_detail`; `POST /v1/extract-claims` answers 429 (quota) or 502 (provider/parse) with **no `claims` key at all**.
  3. **Evidence status split.** `fetched` / `parsed` / `locator_resolved` / `usable_for_claim`; `checksCompleted` is derived from the four material conditions, so an unreadable source reaches `INDETERMINATE`, never `UNSUPPORTED`. A dead-link *fetch* failure is deliberately excluded from that derivation and the reason is in the code.
  4. **Revocation full purge.** `deleteEvidencePayload` nulled only `payload_ref`/`payload_hash` and left `resolved_text` — the column the orchestrator work added later and never reconciled — fully populated, while the resolver never checked revocation at all. Now one transaction shreds every representation, sets `retrieval_status = 'revoked'`, and stamps `payload_revoked_at` on historical matches; the resolver refuses a revoked row on every read path, before any fetch.
  5. **Metered claim extraction.** `checkQuota` before the call (zero network traffic on denial) and a `claim_extraction` usage event after it. Extraction previously imported the cost estimator for a log line and neither gated nor recorded anything.
  - **Two real bugs found by running things, not by reading them:** (a) the PDF parse timeout used an `unref()`'d timer, so if the parse never settled and nothing else was pending the event loop drained and the timeout **silently failed to fire in exactly the case it existed for** — found because the test that drove it deterministically was cancelled rather than passing; (b) `quotaCheck.test.ts` asserted an EXACT global monthly usage total, which had only ever held by luck (no other test file wrote a `usage_event`) and broke the moment extraction started metering — rewritten as a delta assertion, with the exact assertion kept where it is genuinely isolated (the fresh per-org sum).
  - **Suite: 270 tests, 266 pass, 0 fail, 4 skipped** (from 215/211/0/4). `typecheck` and `build` clean. Left unstaged.
  - **NOT fixed here, flagged for a follow-up pass — `server/src/engineClient.ts`** (a sibling component this pass was scoped out of): `materialClaims.length === 0` still returns `no_issue`, and `if (result === undefined) continue` still drops a failed claim submission with no finding recorded. The engine now emits exactly the lifecycle signals that fix needs.
  - **Honest caveat found while verifying item 5:** `estimateDeepSeekCostCents` rounds each call to whole cents, so a typical extraction or judge call meters as 0 cents and never accumulates toward either cap. Pre-existing and shared by both model call sites, but it limits how much the new gate actually bites.

**Still correctly not started, no change:** Phase 2 repeat-value measurement, Phase 3 expansion, § Exploratory review, legal docs, support, status page, developer docs, transactional email, account recovery. See `docs/build/tier-1-build-and-operating-plan.md § Delivery sequence` if the reason for that ordering needs re-justifying to anyone.

## Hard constraints — do not relitigate these, they're locked decisions

- **Card copy is locked language** (§ Phase 0 build guide § 0.5, § Product contract) — don't paraphrase the four scenario strings or the card states (`no_issue` / `issue_found` / `could_not_check`). No fifth state, no score, no percentage, no badge.
- **Bounded evidence only, never open-web search** (§ Cost-control rules, § Verification pipeline step 4). This is what distinguishes Notary Check from general fact-checkers like Lenz — don't add retrieval "to make it smarter."
- **The judge never decides the final state** (§ Judge authority boundary, § step 8's unmoving-filter invariant). It outputs `present` / `absent` / `ambiguous` / `cannot_be_determined` per field; deterministic code compares. No confidence-number gating — that was tried and explicitly removed for being inconsistent with the plan's own claims about LLM calibration.
- **v1 evidence classes are narrow on purpose** (§ Document-class scope for v1) — HTML/PDF corporate/financial reports and excerpts traceable to one. Anything else is `INDETERMINATE / out_of_supported_evidence_class`, not silently accepted.
- **False-supported is the error that matters most** (§ Locked test suite and release gates) — a change that improves recall but worsens false-supported or wrong-source acceptance does not ship without an explicit decision.
- **Notary Check is a separate codebase from `notary-platform`** — don't import or depend on anything from that repo; it's a different, unrelated product.

## What to hand a coding agent to actually finish building

1. **This file, plus the full plan document.** Without the plan, an agent will re-derive (and likely get wrong) the card states, the judge design, and the applicability rules — all of which are already fully specified.
2. **This repo's URL** — `https://github.com/notarydev/notary-check` (private) — and `git clone` access.
3. **A Claude account with connector/MCP App development access enabled**, to actually register the tunnel URL and test live (§ 0.1, § 0.11).
4. **A tunnel tool** — `cloudflared` or `ngrok` installed — for § 0.10.
5. **For Phase 1 only, not now**: a DeepSeek API key (§ LLM judge design), a Postgres connection and object store (§ Core data model, § Architecture), and — before real users, not before — the legal package named in § Public-launch readiness.
6. **Explicit scope for the session**: tell it whether you want it to finish Phase 0 (tunnel + live connector test + user tests) or start Phase 1's build order. Don't hand over "finish building" unscoped — the plan is large enough that an agent without a stated boundary will guess, and Phase 0's remaining steps (tunnel, live test, user interviews) are a fundamentally different kind of work than Phase 1's engineering.

## On training/test data for the engine

Short answer: **yes to test/eval data, no to fine-tuning a model** — and the plan already designed for the first, not the second.

**What the architecture actually needs is a labeled held-out evaluation set, not training data.** Notary Check's judge (DeepSeek) is used via prompting — narrow field-extraction questions, Chain-of-Verification-style — not fine-tuned. The deterministic verifier is code, not a trained model. So "make the engine stronger" here means two concrete things, both already specified:

1. **Build the § Locked test suite's 18 test packets now** — these are synthetic/adversarial fixtures (exact support, contradiction, wrong entity, wrong denominator, prompt-injection-in-evidence, adversarial source ingestion, idempotency, etc.). They don't need real users or real data; they can and should be built alongside Phase 1's engineering, per § Phase 1 build order step 3 (adversarial fixtures before the judge is added).
2. **Build a held-out labeled set via the annotation protocol** (§ Evaluator governance and rollback) — two independent annotators per packet, a written claim-boundary/applicability guide, blinded adjudication for disagreements. This becomes the standing regression suite that gates every judge prompt/model change (§ Evaluator governance) and supplies the actual X/Y numbers for the § Pre-pilot engine gate (false-supported rate, wrong-source acceptance, contradiction precision) — those thresholds were deliberately left blank in the plan until this set exists.

**What not to do:** don't feed real customer/user documents into any training or fine-tuning pipeline — § Security, privacy, and reliability requirements already states "keep customer payloads out of development, evaluation, and model-training datasets without explicit agreement," and that's a hard constraint, not a suggestion. If a genuinely stronger judge later requires fine-tuning a model (rather than better prompting), that's a real architecture change beyond what this plan specifies — treat it as its own decision requiring its own data-governance plan, not something to fold in casually while building Phase 1.
